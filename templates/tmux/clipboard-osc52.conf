# OSC52 clipboard bridge for SSH -> tmux -> host terminal clipboard
set -g allow-passthrough on
set -s set-clipboard on
set -as terminal-features ",xterm-256color:clipboard"
set -as terminal-features ",xterm*:clipboard"
